CREATE TABLE customers_raw (
    customer_id      INTEGER,
    email            VARCHAR(255),
    phone            VARCHAR(50),
    country_code     VARCHAR(50),
    updated_at       TIMESTAMP,
    created_at       TIMESTAMP
);
